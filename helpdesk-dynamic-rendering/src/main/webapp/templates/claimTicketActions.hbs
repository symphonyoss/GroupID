<messageML>
    {{#if showClaim}}
        <action id="claimTicket" class="tempo-btn tempo-btn--good"/>
    {{else}}
        <label class="label">Claimed by: {{userName}}</label>
    {{/if}}
</messageML>